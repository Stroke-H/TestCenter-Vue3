# Legacy Data Backup

This directory stores archived `.jsonl` snapshots that were kept after the project migrated business data to MySQL.

They are not part of the normal runtime read/write path for migrated modules.

Keep these files only as historical backup or emergency recovery material.
