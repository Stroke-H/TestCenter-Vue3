# TestCenter 开发者规则

本文件是 TestCenter 项目的顶级协作规则。所有 AI 助手、自动化 Agent 和人工开发者在修改本仓库时，都应优先遵循这些规则。

## 一、项目边界与技术栈

1. **前端技术栈固定**：前端使用 Vue 3、TypeScript、Vite、Pinia、Vue Router、Element Plus。
2. **后端技术栈固定**：后端使用 Go、Gin、MySQL，并以 HTTP/WebSocket API 为前端提供能力。
3. **性能与工具链**：性能压测以 K6 为主，报告展示以平台内页面或 iframe 承载。
4. **架构原则**：保持视图层、状态层、服务层、数据层职责分离。不要把接口请求、复杂业务判断和 UI 渲染混在同一段逻辑里。
5. **禁止引入无关技术栈**：除非用户明确要求，不要新增大型框架、状态库、构建工具或跨端技术。

## 二、执行工作流

1. **先理解再动手**：修改前先阅读相关文件和现有实现，不凭记忆猜测代码结构。
2. **简单优先**：优先选择最小、直接、可验证的方案。不要为了展示能力而过度抽象。
3. **手术式修改**：每一行改动都应服务于当前用户需求。禁止顺手重构、顺手格式化、顺手删除无关代码。
4. **保留用户改动**：工作区可能包含用户或其他 Agent 的未提交修改。不要回滚、覆盖或清理自己没有确认来源的改动。
5. **遇到高风险操作先暂停确认**：涉及历史重写、强推、批量删除、数据库结构调整、权限模型调整、数据迁移、密钥配置变更时，必须先列计划并等待用户确认。
6. **失败要继续定位**：遇到报错时，优先追踪根因，不要只做表面兜底。无法继续时要说明阻塞点和已验证的信息。

## 三、代码修改规范

1. **Vue 代码规范**：优先使用 Composition API、`<script setup>` 和 TypeScript。保持响应式数据来源清晰，避免不必要的全局状态。
2. **Pinia 规范**：跨页面共享状态放入 Pinia；组件局部状态留在组件内。持久化状态必须考虑登录态、权限和数据过期。
3. **Go 代码规范**：保持 handler、service、model 分层。handler 负责参数和响应，service 负责业务逻辑，model 负责数据结构和持久化映射。
4. **错误处理**：前后端都必须显式处理异常。前端要给出用户可理解的提示，后端要返回稳定的错误结构并记录必要日志。
5. **命名规范**：变量和函数使用 `camelCase`，类型、组件、接口使用 `PascalCase`。文件和目录命名必须语义化。
6. **注释规范**：只在复杂业务、边界条件、权限判断、并发流程、数据迁移、兼容逻辑处写解释性注释。不要写重复代码字面含义的废话注释。
7. **UI/UX 一致性**：优先延续平台已有视觉语言。卡片、按钮、间距、状态色、空状态和加载态要统一、克制、清晰。
8. **权限与登录态**：权限控制必须同时考虑前端展示和后端校验。不要只靠隐藏入口保护功能。
9. **新增卡片必须接入权限系统**：任何新增的仪表盘卡片、功能入口卡片、工具分区卡片、报告入口卡片或设置入口卡片，都必须同步新增或复用明确的 `permissionKey`，并接入前端权限枚举、后端默认权限、路由 `meta.permissionKey`、卡片显隐判断和最近使用缓存过滤；如果卡片背后存在可执行接口，后端接口也必须做同权限或更细粒度权限校验。禁止新增“只在 UI 上可见但权限系统不可配置”的卡片入口。
10. **局域网固定访问规则**：用户可见入口必须优先使用固定局域网主机名，例如 `http://strokeh.local:5173/dashboard`。不要在前端页面、报告 URL、WebSocket、iframe、下载链接、飞书回链中硬编码 `localhost`、本机 IP 或临时 IP。
11. **运行时地址统一生成**：前端需要访问后端 HTTP、WebSocket、静态报告时，必须使用 `src/utils/runtimeUrl.ts` 的 `buildBackendUrl`、`buildBackendWsUrl`、`normalizeBackendUrl`；后端生成平台链接时，必须使用 `server/services/platform_url.go` 的 `PlatformFrontendURL`、`PlatformBackendURL`。只有 Vite 本机代理、启动脚本健康检查这类纯本机内部逻辑可以使用 `127.0.0.1`。
12. **固定访问链路自检**：涉及登录、报告查看、脚本运行、WebSocket、iframe、飞书消息链接、下载链接的改动，交付前必须扫描是否新增了 `localhost:8080`、访问者侧 IP 拼接或散落的 `window.location.hostname + ':8080'` 写法，并验证 `strokeh.local` 下关键接口可访问。

## 四、验证闭环

1. **修改后必须自检**：代码修改完成后必须尽可能运行对应验证，再交付结果。
2. **前端改动验证**：涉及 Vue、Pinia、Router、样式或构建配置时，优先运行 `npm run build`。
3. **后端改动验证**：涉及 Go 代码、接口、服务、数据库逻辑时，优先在 `server/` 下运行 `go test ./...`。
4. **规则或文档改动验证**：纯文档、规则文件修改至少要读回检查内容，确认结构、语义和格式无误。
5. **无法验证必须说明**：如果因为依赖、网络、环境、数据库连接等原因无法运行验证，必须明确说明原因和风险。
6. **不要伪造验证结果**：没有实际执行的命令，不得声称已通过。

## 五、ChangeLog 规范

1. **功能变更要更新 ChangeLog**：新增功能、删除功能、用户可感知的行为变化、重要 Bug 修复，应同步更新 `changeLog/` 下对应模块记录。
2. **允许合理例外**：纯文档、规则文件、隐私清理、注释调整、无用户行为变化的格式整理，可以不更新 ChangeLog。
3. **记录要面向使用者**：ChangeLog 应说明用户能感知到什么变化，而不是只罗列文件名。
4. **新模块要新建记录**：新增独立模块时，应在 `changeLog/` 下新增对应记录文件。

## 六、GitHub 提交规范

1. **提交前检查暂存内容**：提交前必须确认 `git diff --cached` 中没有无关文件、运行产物、真实密钥或用户隐私数据。
2. **提交信息使用 Conventional Commits**：例如 `feat: add assistant chat mode switch`、`fix: handle ime composition enter key`、`chore: remove tracked runtime secrets`、`docs: update cursor rules`。
3. **单次提交聚焦**：一次提交只表达一个清晰主题。不要把功能开发、隐私清理、格式化、临时调试混在一起。
4. **推送前自检**：推送前应确保工作区状态明确，必要验证已完成或已说明未验证原因。
5. **强推保护**：只有在用户明确确认历史重写或强推时，才允许执行 `git push --force-with-lease`。强推后必须提醒协作者重新 clone 或 reset。
6. **不要改写已发布历史**：除隐私清理、用户明确要求的历史整理外，不要 amend、rebase 或重写已推送提交。

## 七、隐私与数据安全

1. **真实敏感数据禁止入库**：禁止提交 API Key、Token、Cookie、Session、密码、密码哈希、真实用户邮箱、飞书 open_id、设备资产清单、业务备份数据、运行报告。
2. **运行数据只保留本地**：以下内容只能本地保存，不得提交到 GitHub：
   - `server/data/*.json`
   - `server/data/processes/`
   - `drama_info.json`
   - `backup/legacy/*.jsonl`
   - `server/feishu/*.jsonl`
   - `k6-scripts/reports/*`
3. **示例配置替代真实配置**：需要提交配置结构时，只能提交 `*.example.json`、`.env.example` 或无敏感值模板。
4. **发现泄露要先止血**：如果发现敏感数据已经进入 Git 历史，应先移除跟踪、补充忽略规则、提交安全基线，再按用户确认执行历史重写。
5. **密钥暴露后必须轮换**：历史清理不能让已泄露密钥重新安全。已暴露的 key、token、secret 必须在服务侧重新生成。

## 八、数据库与持久化规则

1. **以 MySQL 为主存储**：已迁移到 SQL 的业务数据，不应重新依赖 jsonl 作为主链路。
2. **本地 json/jsonl 仅作配置或备份**：配置、legacy 备份、迁移前数据可以本地保留，但不得重新成为线上主存储。
3. **数据结构调整先给方案**：新增表、删字段、改主键、合并数据源前，必须先说明收益、兼容影响和迁移方式。
4. **用户与权限谨慎处理**：`testers` 是基础用户表。涉及用户 ID、登录态、权限默认值、session、cookie 的改动必须同步考虑前后端兼容。

## 九、沟通与交付

1. **默认中文沟通**：面向用户的计划、解释、交付说明、风险提示默认使用中文。
2. **先结论后细节**：交付时优先说明做了什么、验证了什么、还有什么风险。
3. **不要制造额外工作**：能直接检查、修复、验证的事情不要交给用户；只有涉及产品取舍或高风险操作时才询问。
4. **明确假设**：如果必须基于假设推进，交付时要说明假设内容。
5. **保持上下文连续**：新会话或上下文不足时，应优先读取 `changeLog/`、关键路由、相关 store/service，避免重复踩旧坑。
6. **功能完成后必须执行收尾检查**：任何代码、配置、脚本、文档规则修改完成后，在对用户宣告“已完成”之前，必须先逐项执行一次 `Rule Closeout Checklist`，不得跳过。
7. **收尾检查未完成不得直接结束任务**：如果 `ChangeLog`、验证、风险说明、提交状态中有任一项尚未核对，必须先补齐，或明确说明为什么本次不适用。

### Rule Closeout Checklist

1. **检查是否需要更新 ChangeLog**：凡是新增功能、删除功能、用户可感知行为变化、重要 Bug 修复，必须先判断并更新对应 `changeLog/` 文件；如果不更新，也要确认属于规则允许的例外。
2. **检查是否完成必要验证**：根据改动类型运行对应验证命令；前端优先 `npm run build`，后端优先 `go test ./...`，纯规则或文档至少读回检查。
3. **检查是否存在高风险影响**：如果改动涉及权限、登录态、数据库、数据迁移、密钥、历史重写、强推，交付时必须明确说明影响范围和注意事项。
4. **检查工作区和提交范围**：准备提交时，必须确认暂存内容只包含本次目标相关文件，没有调试产物、敏感信息或无关改动。
5. **检查最终回复是否完整**：最终回复至少应说明本次做了什么、验证了什么、哪些项未验证、是否更新了 `ChangeLog`、是否已提交或推送。

## 十、禁止事项

1. 禁止提交真实密钥、运行数据、用户隐私和临时报告。
2. 禁止在未确认的情况下删除、重置、覆盖用户已有改动。
3. 禁止为了修一个 Bug 大范围重构无关模块。
4. 禁止只改前端隐藏入口而不做后端权限保护。
5. 禁止伪造测试、构建、推送或部署结果。
6. 禁止把调试日志、临时脚本、一次性数据产物混入正式提交。
