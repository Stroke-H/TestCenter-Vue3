# TestCenter 开发者核心规范 (.cursorrules)

根据我们在交流过程中您的直接诉求及沉淀下来的**全局核心开发准则**，本文件为项目顶级规则，任何功能迭代、代码修改或新对话开启都必须严格遵循：

## 一、 技术栈与架构选型
1. **统一前端生态**：前端强制使用 **Vue 3 + TypeScript + Vite**，基于 **Pinia** 进行所有状态（如报告集）的统一持久化与状态管理。UI 层统一采用 **Element Plus** 现代化组件框架。
2. **轻量化中心枢纽**：后端一律使用 **Go + Gin** 构建常驻 HTTP/WebSocket 服务，仅提供工具集的编排（如调用系统进程 `os/exec`）与静态文件映射。
3. **性能并发引擎**：深度集成 **K6** 作为标准性能/接口压测工具，输出标准化的 HTML 图表报告以便前端 iframe 呈现。

## 二、 基础代码格式规范
1. **命名严谨性**：
   - 文件与目录结构保持语义化，组件文件大驼峰 `PascalCase`（如 `TestReports`）。
   - 代码层：变量及防抖必须采用小驼峰 `camelCase`，TypeScript 接口（Interface）与类使用 `PascalCase`。
   - 严禁出现难以阅读和维护的非标准简写。
2. **设计美学 (UI/UX)**：
   - 功能页面必须紧密贴合（或超过）现有平台的 Modern 现代风格和高级感。
   - 页面需包含侧边栏导航、面包屑、状态过滤以及响应式布局等通用骨架。
   - 善用徽章（Badge/Tag）、卡片圆角以及阴影层级，以保证视觉上清晰耐看。

## 三、 执行纪律与核心自检流 (Core Workflow)
1. **每次修改必须自检（最高红线）**：每次代码修改完成后，**必须先编译（或执行脚本）自检一下，确认无报错之后，再交付确认修改**（遵循全局规则的第 10 点）。
2. **ChangeLog 同步更新 (New Rule)**：**任何代码变动或功能增删，必须同时更新 `changeLog/` 文件夹下对应的 ChangeLog 文件**。确保开发历史与代码状态绝对同步。
3. **最小化侵入原则**：在追加新功能或者修复 Bug 时，优先遵循**最小化改动**原则，只需变更业务流相关的代码，不随意推翻无关业务的历史重构。
4. **注释要求清晰度**：对 Vue 的 Composition API 与后台 Go 并发引擎（WebSocket 通信链路等），都要做到逻辑段落逐行注释。

## 四、 自动化生命周期保证
1. **脚本一键闭环**：启动流程中，统一采用 `start.sh` 执行完整的前后缀项目冷启动与依赖监控。
2. **端口僵尸清理**：对诸如 `5173` (Vite) 或 `8080` (Go Gin) 被误占用情况需要实现自动化侦测并清理。

## 五、 沟通与文档语言规范 (Language Requirement)
1. **中文强制性**：为了保证沟通的高效与准确，后续任何生成的 **Implementation Plan (开发计划)** 或 **Walkthrough (交付文档/演示)** 都必须使用 **中文** 进行编写。
2. **任务描述**：面向用户核心交付的文档必须全中文。

## 六、 核心全球开发通用规则 (Global Core Rules)
1. 架构模式：采用 MVVM 架构，视图逻辑与业务分离。
2. 风格规范：Swift 5.9+ (针对相关 Logic), camelCase 变量, PascalCase 类型。
3. 性能审计：Instruments 扫描，目标 >60 FPS，主线程阻塞 <10ms。
4. 测试覆盖：单元测试 85%，UI 测试 60%。
5. 修复策略：只改 Bug 相关代码，不变业务语义。
6. 自检编译：修改完成后必须先编译，确认无误后再交付。
7. 反重力 Agent 优化：高负载并发处理、精度控制、资源自动释放。

## 七、 跨对话上下文初始化规则 (Session Initiation Rule)
1. **强制背景读取**：**每次开启新的对话框或在无上下文情况下，AI 助手必须首先自动读取 `changeLog/` 文件夹下的所有文件**。
2. **目的**：通过阅读变更记录快速熟悉项目当前进度、模块分布及历史变更点，确保后续决策的连贯性。

## 八、 修改红线
- 禁止强制解包，必须异步处理错误。
- 严禁重构与当前任务无关的稳定代码。

## 九、GitHub 提交与隐私规范
1. **提交前必须检查隐私数据**：提交前必须确认不包含 API Key、Token、Cookie、Session、密码、密码哈希、真实用户邮箱、飞书 open_id、设备资产清单、运行报告、业务备份数据。
2. **运行数据禁止入库**：以下内容只能本地保存，禁止提交到 GitHub：
   - `server/data/*.json`
   - `server/data/processes/`
   - `drama_info.json`
   - `backup/legacy/*.jsonl`
   - `server/feishu/*.jsonl`
   - `k6-scripts/reports/*`
3. **示例配置替代真实配置**：需要提交配置结构时，只能提交 `*.example.json`、`.env.example` 或无敏感值模板。
4. **提交信息规范**：提交信息使用 Conventional Commits 风格，例如：
   - `feat: add assistant chat mode switch`
   - `fix: handle ime composition enter key`
   - `chore: remove tracked runtime secrets`
   - `docs: add github history cleanup guide`
5. **单次提交聚焦**：每次提交只包含一个清晰主题；不要把隐私清理、功能开发、运行产物混在一起。
6. **强推保护**：只有在用户明确确认历史重写时，才允许执行 `git push --force-with-lease`；强推后必须提醒协作者重新 clone 或 reset。
